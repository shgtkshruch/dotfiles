---
- hosts: localhost
  connection: local
  gather_facts: no
  sudo: no
  roles:
    - dotfiles
    - karabiner
    - peco
    - {role: vim, tags: ['vim']}
    - homebrew
    - docker
    - packages
